#
# List of encrypted disks
#
DISKS="sda1"

#
# User to receive notifications of unauthorized boots.
#
#MAILTO="root"

#
# Remote directory for rsync backup of the keys
# <user>@<host>:<path>
#
#BACKUP="user@host:path"

#
# Picture to display in case of unauthorized access.
# The path is relative to the token_mgmt configuration directory.
#
PICTURE="Z267.png"

#
# Display parameters for the picture:
#
#  --stretch      | -f: Strech (using a simple resizing routine)
#                       the image to fit onto screen if necessary
#  --colorstretch | -k: Strech (using a 'color average' resizing routine)
#                       the image to fit onto screen if necessary
#  --enlarge      | -e: Enlarge the image to fit the whole screen if
#                       necessary
#  --ignore-aspect| -r: Ignore the image aspect while resizing
#
PICTURE_PARAMS=""
